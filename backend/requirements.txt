# Web 框架
fastapi==0.115.6
uvicorn[standard]==0.34.0
websockets==14.1

# 配置与校验
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# 数据库（SQLAlchemy 异步，本地起步用 aiosqlite，线上切 Supabase/PostgreSQL 用 asyncpg）
sqlalchemy[asyncio]==2.0.36
aiosqlite==0.20.0
asyncpg==0.31.0
alembic==1.14.0

# 认证
pyjwt==2.10.1
bcrypt==4.2.1

# 工具
httpx==0.28.1
python-multipart==0.0.20

# 火山·端到端实时语音 SDK(协议帧拼装 + 数据模型,免去手写二进制协议)
volcengine-audio==0.2.1
