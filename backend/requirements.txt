# Web 框架
fastapi==0.115.6
uvicorn[standard]==0.34.0
websockets==14.1

# 配置与校验
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# 数据库（SQLite 起步，SQLAlchemy 保持可移植；后切 MySQL 时加 aiomysql）
sqlalchemy[asyncio]==2.0.36
aiosqlite==0.20.0
alembic==1.14.0

# 认证
pyjwt==2.10.1
bcrypt==4.2.1

# 工具
httpx==0.28.1
python-multipart==0.0.20

# 火山·端到端实时语音 SDK(协议帧拼装 + 数据模型,免去手写二进制协议)
volcengine-audio==0.2.1
