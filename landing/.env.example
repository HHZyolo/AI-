# 前端环境变量示例。复制为 .env.local（开发）或在 Vercel Project Settings → Environment Variables 配置（线上）。
# Vite 规则：只有 VITE_ 前缀的变量才会被注入到浏览器代码里。

# ── 后端 HTTP API 地址 ──
# 本地开发（同机）：http://127.0.0.1:8000
# 线上（腾讯云）：http://124.222.164.211:8000
#   ⚠️ 如果前端跑在 https://*.vercel.app，浏览器会拦截向 http:// 的请求（Mixed Content）
#   需要先给后端套 HTTPS（Caddy / Nginx + Let's Encrypt 或 Cloudflare Tunnel）
VITE_API_BASE=http://127.0.0.1:8000

# ── WebSocket 地址（实时语音通话用）──
# 本地：ws://127.0.0.1:8000
# 线上：ws://124.222.164.211:8000，套了 HTTPS 后改成 wss://your-domain
VITE_WS_BASE=ws://127.0.0.1:8000
